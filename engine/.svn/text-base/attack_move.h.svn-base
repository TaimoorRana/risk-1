#ifndef ATTACK_MOVE
#define ATTACK_MOVE

class Attack_Move{
    Attack_Move();
    ~Attack_Move();
    Country* origin;
    Country* destination;
    int number_of_armys;
    int* attack(Country* origin, Country* destination);
};

#endif // ATTACK_MOVE

